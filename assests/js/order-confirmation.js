(function () {
  "use strict";
  var root = document.querySelector("[data-order-confirmation]"); if (!root) return;
  function esc(v) { return String(v == null ? "" : v).replace(/[&<>"']/g, function (m) { return { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[m]; }); }
  var order; try { order = JSON.parse(sessionStorage.getItem("lgndry_last_order") || "null"); } catch (_error) { order = null; }
  var expected = new URLSearchParams(location.search).get("order");
  if (!order || (expected && order.orderNumber !== expected)) { root.innerHTML = '<section class="confirmation-card"><span>Order confirmation</span><h1>Your order has been received.</h1><p>For privacy, this summary is no longer available in this browser. Signed-in customers can review their orders in My Account.</p><div><a class="commerce-primary" href="account.html#orders">My Account</a><a class="commerce-text-link" href="collection.html">Return to collection</a></div></section>'; return; }
  var total = order.grandTotal != null ? order.grandTotal : Number(order.subtotal || 0) + Number(order.deliveryFee || 0);
  root.innerHTML = '<section class="confirmation-card"><span>' + esc(order.orderType || "Order") + ' received</span><h1>Thank you,<br>' + esc(String(order.customerName || "").split(" ")[0]) + '.</h1><p>Your order <strong>' + esc(order.orderNumber) + '</strong> has been sent to the LGNDRY.Co studio. A confirmation will be sent to <strong>' + esc(order.customerEmail) + '</strong>.</p><dl><div><dt>Order total</dt><dd>' + esc(window.LgndryCommerce ? window.LgndryCommerce.money(total) : "R " + total) + '</dd></div><div><dt>Payment method</dt><dd>' + esc(order.paymentMethod) + '</dd></div><div><dt>Payment status</dt><dd>' + esc(order.paymentStatus) + '</dd></div><div><dt>Delivery</dt><dd>' + esc(order.deliveryMethod) + '</dd></div></dl><p class="confirmation-note">The studio will confirm availability, delivery, and EFT instructions before fulfilment. No card details have been collected.</p><div><a class="commerce-primary" href="account.html#orders">View My Orders</a><a class="commerce-text-link" href="collection.html">Continue exploring</a></div></section>';
}());
